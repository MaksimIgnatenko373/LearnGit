package Module1.Exams;

import java.util.Arrays;

public class Exam9 {
    public static void main(String[] args) {
        System.out.println();
        int numbers[] = {1,2,3,4,5};
        for (int i = 0; i < numbers.length ; i++) {
        System.out.println(numbers[i]); // Arrays.toString(numbers) - вывод элементов массива в терминале в виде масива. Через "for" в таком виде выдаст несколько раз!!!
        }
    }
}
